export const AUTH_TYPE = 'api-auth';

export const AUTH_HEADER_NAME = 'X-Api-Auth'.toLowerCase();

export const DEFAULT_API_PASSWORD = 'abc123456';

export const DEFAULT_USER_CREDS = {
    id: 1,
    name: "Ironman"
};

export const LOGGER_LEVEL = {
    local: 'debug',
    test: 'error',
    dev: 'debug',
    prod: 'warn'
};

export const DATE_FORMAT = {
    FULL_US_DATETIME_FORMAT: 'MM-DD-YYYY HH:mm:ss'
}

export const ENVIROMENT = {
    LOCAL: 'local',
    DEVELOP: 'dev',
    PRODUCTION: 'prod',
    TEST: 'test'
};